<%- include('../partials/header.ejs') %>
<div class="container">
<h3>
    <%= catalelog.name %>
</h3>

<ul>
    <% catalelog.beer.forEach(function(beer) { %>
        <li>
            <%= beer.name %>
            <form action="/beers/<%= catalelog.id %>/<%= beer.id %>?_method=DELETE" method="POST">
                <button class="btn-small red waves-effect waves-light" type="submit">Delete</button>
            </form>
            </a>
        </li>
        <% }); %>
</ul>

<form action="/beers/<%= catalelog.id %>" method="POST">
    <input type="text" name="name" autocomplete="off">
    <button class="btn waves-effect waves-light" type="submit">Add Beer</button>
</form>

<a href="/catalelogs/<%= catalelog.id %>/edit">
    <button class="btn waves-effect waves-light">Edit cat-ALE-log</button>
</a>
</div>

<%- include('../partials/footer.ejs') %>